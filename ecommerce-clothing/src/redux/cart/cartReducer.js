import {} from "./actions.types";

const INITIAL_STATE = {
	items: [],
};

export const cartReducer = (state = INITIAL_STATE, { type, payload }) => {
	switch (type) {
		default:
			return state;
	}
};
